{
  "name": "zentra-monorepo",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.7.4"
  },
  "lint-staged": {
    "Backend/src/**/*.js": "prettier --check",
    "Frontend/src/**/*.{ts,tsx,js,jsx}": "prettier --check"
  }
}
